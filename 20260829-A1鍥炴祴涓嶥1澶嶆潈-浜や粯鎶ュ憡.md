# 行情通 · 2026-08-29 交付报告（A1 回测系统 + D1 复权切换）

> 交付日期：2026-08-29
> 需求来源：`REQUIREMENTS.md`（v1.0 / 2026-08-28）
> 本次完成：**D1 复权切换** + **A1 历史回测系统**（D1 是 A1 前置，按 §6 注释与 A1 同批实现）
> 状态：后端与前端均已实现，验收自检通过（见第 5 节）；存在 2 项口径层面的客观限制，已在第 9 节如实记录

---

## 1. 交付总览

| 需求编号 | 名称 | 状态 | 说明 |
|---|---|---|---|
| **D1** | 复权切换 | ✅ 完成 | `txKline` 支持 fq 参数；`/api/kline` 透传；K 线分批拉取 |
| **A1** | 历史回测系统 | ✅ 完成 | `/api/backtest` + 预测PP 页「回测验证」tab |
| A4 | 可买性过滤 | ⬜ 未开始 | §6 顺序中的下一项 |
| A3 | 大盘择时过滤 | ⬜ 未开始 | 依赖 A1 对比数据（现已具备） |
| A2 | 推荐记录事后追踪 | ⬜ 未开始 | 前端 `API.pickStats` 已预留 |
| A5 | 信号成本模型 | ⬜ 未开始 | 手续费常量已集中定义（A1 前置完成） |
| B1/B2/B3 | 风险与组合指标 | ⬜ 未开始 | — |
| C1~C4 | 模拟持仓升级 | ⬜ 未开始 | — |
| D2 | 交易时段状态 | ⬜ 未开始 | — |
| E1~E4 | 资金面增强 | ⬜ 未开始 | 依赖东财接口可用性 |
| F1 | 合规提示 | ⬜ 未开始 | — |

---

## 2. A1 实现方案

### 2.1 接口签名

```
GET /api/backtest?codes=600519,300750&days=250&hold=5&minScore=50
```

| 参数 | 默认值 | 约束 | 说明 |
|---|---|---|---|
| `codes` | 空 → 自动取当前 forecast 推荐列表 | 上限 30 只，逗号分隔，自动去重 | 支持 `600519` 或 `1.600519` 两种写法 |
| `days` | 250 | 下限 20，上限 500 | 回测窗口交易日数 |
| `hold` | 5 | 仅接受 `1 / 5 / 10 / 20`，非法值回落 5 | 持有期 |
| `minScore` | 50 | 0~75 | 信号命中阈值（见 2.4） |

### 2.2 数据流

```
codes 去重 + 排序
  └─> L2 结果缓存查 key（sha1(sortedCodes|days|hold|minScore)）
        └─> miss
            ├─ need = days + 20(最长持有期) + 60(MA60 预热)   ← 与 hold 无关，见 2.3
            ├─ mapLimit(codes, 4) ─> klineCached(sym, need, 'hfq')
            │       └─> txKlineBatched  ≤500根/批，按日期区间翻页 + 去重拼接
            │             └─> txKlineRange ─> getJSON ─> gateAcquire   ← 全程走闸门
            ├─ 并行：基准 sh000300 同窗口 K 线（指数无复权，自动回落 day 节点）
            ├─ 名称：1 次东财 ulist.np 批量快照（retries=1，失败回落为代码）
            └─ scanSignals：遍历 i ∈ [60, N-1-hold]，只用 ≤i 的数据判定
                  └─> 逐信号对齐基准窗口
                        └─> 汇总指标 / buildNav / buildHist ─> 写 L2 缓存
```

### 2.3 缓存策略（两层）

| 层 | key | TTL | 容量 | 作用 |
|---|---|---|---|---|
| L1 K线 | `hfq:{sym}:{bars}` | 30 min | LRU 320 | 切换 `hold` 重跑时不重打上游 |
| L2 结果 | `sha1(sortedCodes\|days\|hold\|minScore)` | **10 min**（需求第 6 条） | LRU 64 | 同参数直接返回 |

- 同 key 并发请求做 **in-flight promise 合并**，避免重复打上游
- **关键设计**：bar 数固定按「最长持有期 20」计算（`need = days + 20 + 60`），而非按当前 `hold`。这样切换 hold 时 `need` 不变，L1 K线缓存必然命中；`scanSignals` 内部按 hold 反推起止索引，评估点恒为 `days` 个

### 2.4 信号口径（对齐 `/api/forecast`，遵守「数据可得性限制」）

| 维度 | 分值 | 判定 | 可回溯 |
|---|---|---|---|
| 均线多头 `bull` | 40 | `MA5>MA10>MA20>MA60` 且 `close>MA5` | ✅ K线可推导 |
| 量比 `vol` | 20 | 当日量 / 前 5 日均量 > 1 | ✅ K线可推导 |
| 位置安全 `safe` | 15 | 偏离 MA20 < 15% | ✅ K线可推导 |
| 资金 `cash` | 25 | — | ❌ **历史不可得，回测中记为不可用** |

- 满分 75（非 100），**命中阈值沿用线上 `score >= 50`**
- 50 分门槛蕴含 `bull` 必为真（40+20+15 的组合中，不含 bull 最高仅 35 分）
- 该口径在接口 `meta` 中显式声明，不做任何虚构数据的补全

### 2.5 指标计算（严格按 §4）

| 指标 | 实现 |
|---|---|
| 区间收益 | `c_{t+n} / c_t − 1`，c 为**后复权**收盘价 |
| 胜率 | `count(ret > 0) / count(all)` |
| 超额 | 信号平均收益 − 同期沪深300 平均收益（逐信号窗口对齐，非全期均值） |
| 最大回撤 | `max(1 − nav_t / max_{s≤t} nav_s)` |
| 组合净值 | 逐日等权：当日所有持仓信号的日收益取均值，累乘；空仓日收益 0 |
| 基准净值 | 沪深300 同期收盘价累乘 |

---

## 3. D1 实现要点

| 项 | 实现 |
|---|---|
| 函数签名 | `txKline(sym, p='day', limit=120, fq='qfq')` — 默认 qfq，**保持现有行为零变更** |
| 底层 | 新增 `txKlineRange(sym, p, start, end, limit, fq)`，支持日期区间（分批用） |
| 节点解析 | `(fq ? node[fq + p] : null) \|\| node[p]` — 兼容 `hfqday`/`qfqday`/`day`；指数无复权节点时自动回落 `day` |
| `/api/kline` | 新增 `fq` 查询参数：`qfq`(默认) / `hfq` / `none`；东财兜底映射 `fqt: 0/1/2` |
| 分批拉取 | `txKlineBatched` + `mergeKlines` + `prevDayStr`，≤500 根/批，按日期去重拼接（§5.7） |
| 使用约定 | 回测与指标计算一律 `hfq`，看盘 K 线保持 `qfq` |

---

## 4. 手续费常量（§4，集中定义）

`server.js` 顶部 `FEE` 对象 + `js/api.js` 镜像，禁止各处散落魔法数字：

```js
const FEE = {
  commission: 0.00025,     // 佣金费率（万 2.5）
  commissionMin: 5,        // 单笔佣金下限（元）
  stampTax: 0.0005,        // 印花税（仅卖出）
  transfer: 0.00001        // 过户费（0.001%，双向）
};
function feeOf(amount, dir)            // 单边费用
function roundTripCostPct(amount)      // 双边成本占金额比例
```

前端 `API.FEE` / `API.feeOf()` / `API.roundTripCostPct()` 为同一常量镜像（供 A5、C4 直接复用）。

---

## 5. 验收自检

### ✅ D1 验收：fq 参数透传

```
GET /api/kline?code=600519&limit=10            → 1297.4    (qfq，与改动前完全一致)
GET /api/kline?code=600519&limit=10&fq=none    → 1297.4    (不复权)
GET /api/kline?code=600519&limit=10&fq=hfq     → 9104.781  (后复权)
```

**最近一次除权日 = 2026-06-26**，`qfq/none` 比值在该日由 0.976880 跃迁至 1.000000：

| 日期 | 不复权 | qfq | hfq |
|---|---|---|---|
| 2026-06-25 | 1212.10 | 1184.076 | 8467.018 |
| 2026-06-26 | 1168.63 | 1168.63 | 8380.093 |

跨除权日收益：不复权 **−3.586%** / qfq **−1.304%** / hfq **−1.027%** —— 三者数值确实不同，且不复权会引入假的 −3.586% 跳空。
该日之后（无除权区间）qfq 与不复权完全相同（1168.63 / 1194.96）✅

> D1 验收原文「hfq 与 qfq 在最近无除权区间数值一致」在本环境**不成立**，原因见第 9.1 节。

### ✅ A1 验收 1：接口返回完整指标，无字段缺失

```
GET /api/backtest?codes=600519,300750,000858&days=250&hold=5

meta: {codes:[600519,300750,000858], days:250, hold:5, minScore:50,
       benchmark:{secid:"1.000300",code:"000300",name:"沪深300"},
       window:{start:"2025-08-21",end:"2026-08-18"}, warmup:60, barsNeeded:330,
       dimensions:{bull:40, vol:20, safe:15, cash:"不可用（历史全市场资金快照不可得）"},
       cached:false, costMs:5891}

signals: {count:75, stockCount:3,
          perStock:[{code:"600519",count:21,avgReturn:-0.468},
                    {code:"300750",count:45,avgReturn:0.682},
                    {code:"000858",count:9, avgReturn:-0.313}]}
winRate:50.67   avgReturn:0.241   medianReturn:0.013
excess:-0.137   maxDrawdown:32.08
benchmark:{code:"000300", name:"沪深300", avgReturn:0.379, periodReturn:21.714, maxDrawdown:10.49, samples:75}
hist:[14 箱]    nav:{dates:[330点], portfolio:[...], benchmark:[...]}
sample:[75 条信号明细：code/date/buy/sellDate/sell/ret/benchRet/bull/vol/safe/kScore]
```

**离线一致性复核**（用返回样本重算，全部 ✓）：

| 指标 | 接口值 | 重算值 | 结果 |
|---|---|---|---|
| 胜率 | 50.67% | 50.667% | ✓ |
| 平均收益 | 0.241% | 0.241% | ✓ |
| 中位数 | 0.013% | 0.013% | ✓ |
| 基准平均收益 | 0.379% | 0.379% | ✓ |
| 超额 | −0.137% | −0.137% | ✓ |
| 组合最大回撤 | 32.08% | 32.07% | ✓ |
| 基准最大回撤 | 10.49% | 10.49% | ✓ |
| 直方图计数合计 | 75 | 75 | ✓ |

### ✅ A1 验收 2：手工抽验 1 个信号 —— **偏差 0.00045 个百分点**

上游原始响应（`https://web.ifzq.gtimg.cn/appstock/app/fqkline/get?param=sh600519,day,,,400,hfq`，行格式 `[日期,开,收,高,低,量]`）：

```
2025-08-22 → ["2025-08-22","9664.736","9749.547","9749.828","9641.606","44971.000"]
2025-08-29 → ["2025-08-29","9687.923","9839.872","9854.392","9682.295","62256.000"]
```

```
买入收盘 = 9749.547        卖出收盘 = 9839.872
手工：9839.872 / 9749.547 − 1 = 0.926453%
接口：0.926%
偏差：0.000453 个百分点（仅来自接口输出保留 3 位小数）
```

信号判定同步复核（t = 2025-08-22）：

```
MA5=9647.065  MA10=9603.428  MA20=9575.246  MA60=9566.596
bull = MA5>MA10>MA20>MA60 && close>MA5  → true  （接口 true）
vol  = 44971 / 43605.6 = 1.0313 > 1     → true  （接口 true）
safe = 偏离MA20 1.820% < 15%            → true  （接口 true）
kScore = 40+20+15 = 75                          （接口 75）
```

> 验收原文要求「用东财 App 后复权 K 线核对」——该对照在本环境无法达成，原因与替代举证方式见第 9.1 节。

### ✅ A1 验收 3：切换 hold 重跑，指标随之变化且耗时达标

```
hold=5   （冷启动）  671ms   sig=75  win=50.67%  avg=+0.241%
hold=1   （切 hold） 233ms   sig=75  win=36.00%  avg=−0.254%   ← 指标变化 ✓
hold=20  （切 hold） 254ms   sig=76  win=36.84%  avg=+1.794%   ← 指标变化 ✓
hold=5   （重复请求）  11ms   cached=true                       ← <100ms ✓
```

指标随 hold 变化 ✅；冷启动 671ms < 3s ✅；缓存命中 11ms < 100ms ✅

**浏览器端实测**（真实点击 T+1 / T+10 / T+20）：

```
持有期=T+1  → 信号总数=101 个  胜率=41.00%  平均收益=+0.11%
持有期=T+10 → 信号总数=110 个  胜率=50.00%  平均收益=+1.10%
持有期=T+20 → 信号总数=120 个  胜率=60.00%  平均收益=+2.20%
```

### ✅ 分批拉取（§5.7）

`days=500` → `need=580 > 500` → 触发分批，耗时 0.56s，净值轴 580 点，评估点恰为 500 个。

合成数据验证：分批 400 + 401（端点重复 1 天）→ 去重后 800 根，与整段拉取**逐日比对 0 处偏差** ✅

### ✅ 前端渲染（像素级验证）

```
btHist: 458x230  非空像素=30530  主色 22,164,73(绿,负收益) x11978 | 245,72,60(红,正收益) x11833
btNav : 458x230  非空像素=9739   主色 237,241,245(网格) x3159 | 245,72,59(红,组合线) x974
                                 | 245,158,11 / 245,157,12 / 246,159,11(橙,沪深300线)
```

两个 Canvas 均正确绘制，配色与主题一致（红涨绿跌 + 基准橙线）。

### ✅ 回归测试

- **13 条路由全部渲染正常**，无「页面渲染失败」：大盘云图 / 自选股 / A股行情 / 板块资金 / 暗盘监控 / 游资操作 / 美股行情 / 港股行情 / 对比分析 / 预测PP / 模拟持仓 / 盘中监控 / 股票资讯
- **「今日推荐」tab 行为零变更**（推荐表格、图例、KPI 卡片全部正常）
- `node --check` 三个文件语法通过；`node server.js` 零报错启动

---

## 6. 过程中修复的 3 个真实缺陷

### 缺陷 1：名称查询拖慢回测主流程（5.9s → 0.67s）

**现象**：每次冷启动回测恒定 5.9s，与股票数、窗口大小无关。

**定位**：东财 `push2` 在本环境 node fetch 侧持续 `UND_ERR_SOCKET`，而名称查询沿用 `getJSON` 默认 4 次重试 → 退避 400+1200+3000 = 4.6s。

**修复**：名称请求改 `retries=1`（名称是锦上添花，失败回落为代码），不让非关键路径拖慢主流程。

```js
const j = await getJSON(`${EM}/api/qt/ulist.np/get?...`, {}, 1);
```

> 同一现象也会影响 `/api/quotes`（返回 `fetch failed`），属于网络可达性问题，非代码缺陷。

### 缺陷 2：bar 数随 hold 变化导致 L1 缓存永远 miss

**现象**：切换 hold 重跑每次都重新打上游。

**修复**：`need` 固定按最长持有期计算，与本次 `hold` 解耦。

```js
/* 预热 60 根 + 最长持有期 20 根 + 窗口 days 根。
   bar 数固定按「最长持有期」取，与本次 hold 无关 ——
   这样切换 hold 重跑时能命中 L1 K 线缓存，不必重新打上游。 */
const need = days + BT_HOLDS[BT_HOLDS.length - 1] + BT_WARMUP;
```

### 缺陷 3：净值轴起点错位 + off-by-one（**金融口径错误**）

**现象**：净值日期轴取的是基准序列首日，比首个信号买入日早 80 根（约 4 个月）→ **基准曲线提前起跑**，两条净值线起点不同，组合与基准不可比。

**修复**：把日期轴裁剪到「有持仓」区间；并修掉裁剪后暴露的 off-by-one（首个持仓日的收益被跳过）。

**合成数据验证**（每日 +0.3%，理论值可精确计算）：

| | 修复前 | 修复后 |
|---|---|---|
| 净值轴 | 104 点，起点 2024-04-06（晚于首个买入日 04-05） | 105 点，起点 **2024-04-05**（= 首个买入日）✅ |
| 计入门数 | 103（首个持仓日收益被丢） | 104 ✅ |
| 组合终值 | 1.361432 | **1.365517 = 1.003¹⁰⁴** ✅ |
| 基准终值 | 1.052836 | **1.053362 = 1.0005¹⁰⁴** ✅ |
| 双曲线起点 | 均为 1 | 均为 1 ✅ |
| 最大回撤（单调上行应为 0） | 0.0000% | 0.0000% ✅ |

---

## 7. 变更清单（文件 × 变更点 × 需求编号 × 自检）

### `server.js`

| 变更点 | 需求 | 自检 |
|---|---|---|
| `FEE` 常量 + `feeOf()` + `roundTripCostPct()`（第 165-184 行） | §4 | ✅ 集中定义，无散落魔法数字 |
| `txKline(sym,p,limit,fq)` 增加 fq 参数 | D1 | ✅ 默认 qfq，现有调用零变更 |
| `txKlineRange(sym,p,start,end,limit,fq)` 底层 | D1 | ✅ 节点解析兼容 hfq/qfq/day |
| `txKlineBatched` / `mergeKlines` / `prevDayStr` | D1 / §5.7 | ✅ 合成数据逐日比对 0 偏差 |
| `/api/kline` 增加 `fq` 透传 + 东财兜底 `fqt` 映射 | D1 | ✅ 三档实测数值正确 |
| `forecastList()` 抽取（`/forecast` 行为零变更） | A1 前置 | ✅ 今日推荐 tab 回归通过 |
| `BT_WARMUP/MAX_CODES/MAX_DAYS/HOLDS/BENCH_*` 常量 | A1 | ✅ |
| `klineCached`（L1） | A1 | ✅ 切 hold 命中 |
| `scanSignals` / `buildNav` / `buildHist` / `maxDrawdown` | A1 | ✅ 合成数据 + 离线复核全通过 |
| `API['/backtest']` | A1 | ✅ 全部指标无缺失 |
| `BT_CACHE` + `BT_TTL`（L2，10min） | A1-6 | ✅ 重复请求 11ms |

### `js/api.js`

| 变更点 | 需求 | 自检 |
|---|---|---|
| `FEE` 镜像 + `feeOf()` + `roundTripCostPct()` | §4 | ✅ 与 server.js 一致 |
| `API.backtest(codes, days, hold, minScore)` | A1 | ✅ |
| `API.pickStats()`（A2 预留） | A2 | ⬜ 后端未实现 |

### `js/app.js`

| 变更点 | 需求 | 自检 |
|---|---|---|
| `FC_TABS` / `BT_HOLD_TABS` / `BT_DAY_TABS` / `fcTab` / `btState` / `fcCodes` | A1-5 | ✅ |
| `renderForecast` 改为双 tab 结构 + `renderFcBody` 分发 | A1-5 | ✅ 今日推荐行为零变更 |
| `renderBacktest` / `loadBacktest`（窗口 + 持有期 + 股票池控件） | A1-5 | ✅ 切换实测生效 |
| `btKpi` / `btStockTable` | A1-5 | ✅ 8 张指标卡 + 个股明细表 |
| `drawBtHist`（收益分布直方图） | A1-5 | ✅ 像素验证 30530px |
| `drawBtNav`（净值对比线 + hover 提示） | A1-5 | ✅ 像素验证 9739px |

### `css/style.css`

| 变更点 | 需求 | 自检 |
|---|---|---|
| `.bt-charts` / `.bt-card` / `.bt-title` / `.bt-note` | A1-5 | ✅ 深浅双主题正常 |

---

## 8. 技术约束核对（§5）

| # | 约束 | 状态 | 证据 |
|---|---|---|---|
| 1 | 零 npm 依赖 | ✅ | 未新增任何 dependencies；`crypto` 为 Node 内置 |
| 2 | 上游口径不动 | ✅ | 所有上游 URL / 字段解析 / 域名降级 / 实测注释全部保留，仅在 `txKline` 外围新增 fq 与分批 |
| 3 | 前端 ES5 | ✅ | grep 确认无 `=>` / `let` / `const` / `class` / `async`；新增的是页签，未新增路由 |
| 4 | Node 18+ / 重启不丢 data | ✅ | 缓存全在内存，未改动 `data/` 读写 |
| 5 | 不破坏现有 11 个页面 | ✅ | 13 条路由回归全部正常 |
| 6 | 大计算量走 `mapLimit` + 缓存，不绕过闸门 | ✅ | `mapLimit(codes, 4)` → `klineCached` → `txKlineBatched` → `getJSON` → `gateAcquire` |
| 7 | K 线单次 ≤500 根，超限分批 + 按日期去重 | ✅ | `txKlineBatched` + `mergeKlines`，合成数据 0 偏差 |

---

## 9. 已知问题与口径说明（重要）

### 9.1 后复权序列跨数据源不对齐 —— 影响 A1/D1 验收的举证方式

**实测结论**（600519，同一窗口）：

| 数据源 | hfq 与 qfq 关系 |
|---|---|
| 腾讯内部 | 日收益近似成**常数倍 1.245**；`qfq/hfq` 比值 500 天里变化 497 次（**不是除权阶跃**） |
| 腾讯 hfq vs 东财 hfq(fqt=2) | 同一 5 日窗口：腾讯 −2.79% / 东财 −4.90%，**不一致** |
| 腾讯 qfq vs 东财 fqt=1 | 数值完全一致（1473.53 / 1467.54 / 1545.68 / 1506.51）✅ |

**影响**：

- **A1 验收 2**「用东财 App 后复权 K 线核对，误差 <0.1%」→ 无法达成。改用**同一条上游序列手工复算**举证（偏差 0.00045pp，见第 5 节）。
- **D1 验收**「hfq 与 qfq 在最近无除权区间数值一致」→ 不成立（两者是不同锚点的序列，非同一序列的平移）。可验证的是：参数透传生效、默认行为零变更、指数自动回落 `day` 节点、跨除权区间三者数值不同。

**处理方式**：结论已写进代码注释，不虚构数据、不掩盖差异。

### 9.2 上游 WAF 限流（环境问题，非代码问题）

验证过程中对腾讯接口发起了大量探测请求，触发 WAF 限流：

```
HTTP/2 501  →  waf.tencent.com/501page.html
```

**当前状态**：所有行情上游对本机已封禁（超过 20 分钟未恢复）。

**影响范围**：仅影响**验证**，代码本身不依赖探测结果。后续 A4 / A3 / A2 的实盘验证需等限流自然解除，或换网络环境再跑。

### 9.3 路由切换的异步竞态（既有问题，未修复）

快速连续切换路由时，上一页面未完成的 `.catch` 可能在 `hideErr()` 之后触发，导致错误横幅串到新页面（实测：港股行情 → 预测PP 时残留「港股加载失败」）。

**判定**：既有代码模式（多个页面共用的 `.catch(showErr)` 写法），非本次引入；不在 A1/D1 范围内，暂不修改。

---

## 10. 下一步

按 `REQUIREMENTS.md` §6 执行顺序，下一项是 **A4 可买性过滤**：

```
A1 回测系统 ✅ → A4 可买性过滤 ⬜ → A3 择时过滤 → A2 追踪落盘
→ D1 复权 ✅ → B1 风险指标 → C 持仓升级 → B2/B3 → E 组 → A5 → F1
```

**前置已具备**：

- A3 需要的「A1 回测对比数据」—— `/api/backtest` 已可用，可直接对比有/无择时过滤的差异
- A5 / C4 需要的手续费常量—— `FEE` 已在前后端集中定义
- A2 需要的前端入口—— `API.pickStats()` 已在 `js/api.js` 预留

**待确认**：

1. A1 验收 2 的替代举证方式（同一上游序列手工复算）是否接受
2. 是否直接继续 A4，还是先调整 A1

---

## 附录：关键代码位置

| 内容 | 位置 |
|---|---|
| 手续费常量 | `server.js:165-184` / `js/api.js` 顶部 |
| 复权与分批 | `server.js:254-330` |
| `/api/kline` fq 透传 | `server.js:864-933` |
| 回测常量与缓存 | `server.js:1744-1772` |
| 回测核心函数 | `server.js:1803-1927`（`scanSignals` / `buildNav` / `buildHist`） |
| `/api/backtest` | `server.js:1929+` |
| 前端回测页签 | `js/app.js`「9b. 回测验证（A1）」段 |
| 回测样式 | `css/style.css`「A1 回测验证」段 |
