# 观潮 ML 预测服务依赖
# 核心（已安装）：numpy / pandas / scikit-learn / scipy / xgboost / flask
# 可选（装上时自动启用，不装也不影响引擎运行）：
#   torch        —— LSTM / Transformer 的 autograd/GPU 版（CPU 版即可，体量大，按需安装）
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
xgboost>=2.0
flask>=3.0
# torch>=2.0   # 可选：序列模型增强，体积大，按需 `pip install torch` 开启
# optuna>=3.0  # 可选：超参自动调优（ModelBundle.tune），按需 `pip install optuna` 开启
