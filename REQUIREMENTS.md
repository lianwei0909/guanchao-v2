# 行情通 · 金融方向优化需求文档

> 版本：v1.0（2026-08-28）
> 目标读者：AI 编码助手 / 后续开发者
> 性质：本文档是唯一需求来源，所有改动以此为准；每项需求附验收标准，完成后须逐条自检。

---

## 1. 项目概况

「行情通」：零依赖 Node.js 股市数据聚合站（A 股 / 港股 / 美股），`node server.js` 直接运行，无构建工具。

| 文件 | 规模 | 职责 |
|---|---|---|
| `server.js` | ~2000 行 | HTTP 服务：静态托管 + `/api/*` 行情代理（东方财富主源，腾讯/新浪/同花顺降级）；云同步（`data/sync/`）；邮件通知（`data/mail.json`） |
| `index.html` | ~220 行 | SPA 壳；侧边栏导航（hash 路由）；弹窗（个股详情 / 信号 / 邮件 / 云同步） |
| `js/api.js` | ~220 行 | 前端数据层：fetch 封装 + localStorage（自选 `hqt.watchlist.v1` / 模拟持仓 `hqt.paper.v1` / 同步昵称 `hqt.sync.v1`） |
| `js/app.js` | ~2120 行 | 路由表 `ROUTES` + 11 个页面渲染 + Canvas 图表（`drawMinute` / `drawKline` 等） |
| `css/style.css` | ~760 行 | 样式（含深浅双主题 CSS 变量） |

### 已有能力（禁止重复实现 / 禁止破坏）

以下模块经过实测踩坑，改动时只允许**调用**与**外围包裹**，不允许修改其内部逻辑与上游 URL/字段口径：

- 上游请求闸门：`gateAcquire`（并发 6 / 间隔 110ms）、`getJSON` 指数退避重试、`CLIST_HOSTS` 域名降级、`mapLimit` 并发受限 map
- K 线：`txKline(sym, period, limit)`（腾讯，当前写死前复权 qfq）、东财 kline 兜底（`/api/kline`，limit ≤ 500）
- 市场码映射：`resolve()` / `secidOf()` / `emSecid()` / `txSymOf()`
- 预测打分：`/api/forecast` 四维打分（均线多头 40 / 主力净流入 25 / 量比 20 / 位置安全 15 / 资金强度加 10，≥50 入选）
- 信号阈值：`/api/signal` 分时偏离均价线，`th = max(0.4%, 1.2σ)`，回落至 th/2 触发
- 指数行情：`/api/indices`（含沪深300 `1.000300`，可作基准）

---

## 2. 优化目标

本项目的优化方向为**金融方向**，效果检测遵循三个维度（优先级从高到低）：

1. **策略有效性**：站内唯一的策略输出「预测PP」目前没有任何回测或追踪证明其有效 → 必须建立可量化的验证体系
2. **指标正确性**：对比/持仓页缺乏风险度量指标（波动率、Beta、夏普、最大回撤）
3. **数据准确性**：复权口径、交易时段判断存在金融口径问题

核心原则：**先造尺子，再改规则**——先实现 A1 回测，之后所有策略参数调整都必须用回测数据对比，禁止无数据支撑的主观调参。

---

## 3. 需求清单

### A 组 · 策略可验证（P0）

#### A1 历史回测系统 ⭐ 最高优先级

**现状**：`/api/forecast` 打分规则从未被验证。

**数据可得性限制（务必先读）**：
- 历史某日的全市场快照（当日成交额榜、当日主力净流入 f62）**不可得**，东财 clist 只返回实时数据
- 因此回测只能覆盖 **K 线可推导的维度**：均线多头（bull）、位置安全（偏离 MA20）、量比近似（当日量 / 5 日均量）；资金维度（cash）无法回溯，回测时记为不可用
- 资金维度的验证由 A2 事后追踪补齐（追踪落盘时资金数据是真实的）

**规格**：
1. 新增 `GET /api/backtest`，参数：`codes`（逗号分隔，默认取当前 forecast 推荐列表；上限 30 只）、`days`（回测窗口，默认 250 交易日，上限 500）、`hold`（持有期天数，默认 5，可选 1/5/10/20）
2. 数据源：腾讯日 K（`txKline` 改造为支持复权参数，见 D1），**必须用后复权（hfq）**；基准取沪深300（`1.000300`）同窗口
3. 模拟逻辑：在历史每个交易日 t，对每只股票计算当时点可得的 bull / safe / 量比信号（与 forecast 同参数），命中则记录「t 日收盘买入 → t+hold 日收盘卖出」的区间收益
4. 输出指标（按第 4 节口径计算）：
   - `signals`：信号总数、覆盖股票数
   - `winRate`：胜率（收益 > 0 的信号占比）
   - `avgReturn` / `medianReturn`：区间收益均值 / 中位数
   - `excess`：信号平均收益 − 同期沪深300 平均收益
   - `maxDrawdown`：按信号等权逐日组合净值计算的最大回撤
   - `benchmark`：同期沪深300 收益与最大回撤
5. 前端：在「预测PP」页顶部加「回测验证」tab，展示指标卡片 + 信号收益分布直方图（Canvas）+ 相对基准的净值对比线；支持改 hold 重跑
6. 结果缓存：内存 TTL 10 分钟，key = codes 排序 hash + days + hold

**验收标准**：
- [ ] `/api/backtest?codes=600519,300750&days=250&hold=5` 返回完整指标，无字段缺失
- [ ] 手工抽验 1 个信号：用东财 App 该股后复权 K 线核对买卖两日收盘价与区间收益，误差 < 0.1%
- [ ] 前端切换 hold=1/5/20 重跑，指标随之变化且请求 < 3s（缓存命中 < 100ms）

#### A2 推荐记录事后追踪

**现状**：每日推荐发完即弃，无法知道实际表现。

**规格**：
1. `server.js` 启动后每交易日 15:30（已有邮件巡检的 `setInterval` 机制可复用）调用一次 forecast 逻辑，将当日推荐全量落盘 `data/picks/YYYY-MM-DD.json`（含 code/name/score/target/support 及当时的完整字段）
2. 新增 `GET /api/pick-stats`：读取 `data/picks/` 全部文件，对每个历史推荐计算 T+1 / T+5 / T+20 实际收益（后复权），输出：总推荐数、各期胜率、各期平均收益、相对沪深300 同期超额、逐月胜率趋势
3. 前端：「预测PP」页加「历史追踪」tab，展示上述统计 + 每日推荐明细表（可展开看事后表现）
4. 落盘文件保留 90 天，更早的自动清理

**验收标准**：
- [ ] 手工放入 2 个伪造的 `data/picks/` 文件（各含 1 只票），`/api/pick-stats` 正确算出 T+N 收益
- [ ] 推荐文件字段与当日 `/api/forecast` 输出一致（资金维度字段来自真实当日数据）

#### A3 大盘择时过滤

**规格**：`/api/forecast` 增加择时条件：沪深300 收盘 < MA20 时，所有候选 `score × 0.5`；前端图例区展示当前择时状态（「多头环境 / 弱势环境」徽标）。**参数上线前必须用 A1 回测对比有/无过滤的差异，把对比数据写进代码注释。**

**验收**：模拟弱市日期（可用回测接口指定窗口）验证降分生效；回测对比数据存在。

#### A4 可买性过滤

**现状**：一字涨停、停牌的票会进推荐，实际无法买入。

**规格**：`/api/forecast` 候选过滤：现价 ≥ 涨停价 × 0.998 的剔除（涨停价用东财 `f51` 或腾讯快照 `limitUp`，候选集来自 clist，用 `f2` 与当日涨停价比较；若涨停价字段不可得，用「昨收 × 1.10（主板）/ 1.20（创业板科创）」近似并在注释中说明）。

**验收**：构造一个涨停股代码，确认不出现在推荐结果中。

#### A5 信号成本模型

**现状**：`/api/signal` 阈值 `max(0.4%, 1.2σ)` 无交易成本概念，阈值内的高抛低吸是「赚价差亏手续费」。

**规格**：
1. 手续费常量集中定义（见第 4 节），双边成本约 0.11%（0.025%×2 佣金 + 0.05% 卖出印花税 + 0.001%×2 过户费，佣金不足 5 元按 5 元）
2. 阈值下限从 0.4% 改为 `双边成本 × 2`（即信号净收益须覆盖两倍成本）
3. 信号弹窗每个触发点展示「预估毛收益（dev 值）− 双边成本 = 预估净收益」

**验收**：阈值下限计算正确；信号列表中出现净收益列。

### B 组 · 风险与组合指标（P1）

#### B1 个股风险指标

**规格**：`/api/compare` 每只股票新增字段（近 250 日、后复权、公式见第 4 节）：`vol`（年化波动率）、`beta`（vs 沪深300）、`sharpe`、`mdd`（最大回撤）。前端对比表格加「风险」列组，红绿着色按指标语义（Beta>1 高弹性、夏普>1 优）。

**验收**：抽 1 只票与理杏仁/同花顺对照 Beta 与年化波动率，误差 < 2%。

#### B2 相关性矩阵

**规格**：`/api/compare` 返回 `corr` 字段：两两相关系数矩阵（近 120 日日收益）。前端对比页底部渲染 N×N 热力表（Canvas 或 table，色阶 −1~+1）。

**验收**：贵州茅台 × 五粮液相关系数应在 0.7~0.95 区间（常识校验）；矩阵对角线为 1。

#### B3 超额收益曲线

**规格**：个股详情弹窗 K 线 tab 旁加「对比基准」开关：净值曲线（当日=1）叠加该股后复权净值与沪深300 净值，显示区间超额。

**验收**：曲线上两点抽样验算净值比值正确。

### C 组 · 模拟持仓升级为纸面交易系统（P1）

**现状**（`renderPaper` / `API.paper*`）：仅 localStorage 持仓 + 浮动盈亏 + 总收益率，无流水、无曲线、无成本。

**规格**：
1. **C1 交易流水**：localStorage 新 key `hqt.trades.v1` 存 `{code,name,dir,price,shares,fee,ts}`；建仓/加仓/清仓均记流水；清仓后持仓删除但流水保留
2. **C2 组合指标**：基于流水 + 现价计算——总投入、市值、已实现盈亏、浮动盈亏、**收益率曲线**（每日收盘记录组合市值快照 `hqt.nav.v1`，Canvas 绘制）、**年化收益、最大回撤、交易胜率**（胜率=盈利清仓笔数/总清仓笔数）
3. **C3 基准对比**：曲线叠加同期沪深300 净值，展示超额收益
4. **C4 手续费**：买卖均计费（常量见第 4 节，佣金最低 5 元），建仓弹窗展示预估费用；已实现盈亏按「卖出净额 − 买入总成本」计

**验收**：手工构造「100 股 × 10 元买入 → 11 元卖出」，验算费用、已实现盈亏、胜率（100%）；曲线出现两笔交易对应拐点。

### D 组 · 数据口径修正（P2）

#### D1 复权切换

**现状**：`txKline` 写死 qfq。前复权的历史价在每次除权后整体平移，用它回测会产生假买卖点。

**规格**：`txKline(sym, period, limit, fq)` 增加 fq 参数（'qfq' | 'hfq' | ''，默认 'qfq' 保持现有行为）；`/api/kline` 增加 `fq` 查询参数透传；**所有回测/指标计算（A1/A2/B 组）一律用 hfq**，看盘 K 线保持 qfq。注意腾讯接口 fqkline 的 param 末段即复权参数，hfq 时返回节点 key 为 `hfq+period`，解析处需同步兼容（现有代码 `node['qfq'+p] || node[p]`）。

**验收**：`/api/kline?code=600519&fq=hfq` 与 qfq 在最近无除权区间数值一致；跨越最近一次除权日的区间两者数值不同。

#### D2 交易时段状态

**规格**：先核实 `mktStatus`（app.js 顶部时钟旁徽标）当前按哪个市场判断；改为 A 股 / 港股 / 美股三市场独立状态徽标（A股 9:30-11:30/13:00-15:00，港股 9:30-12:00/13:00-16:00，美股按北京时间 21:30-4:00 夏令时 22:30-5:00，节假日不要求精确）；前端自动刷新频率跟随：持仓页 `every(10000)` 在所有市场休市时降为 60s。

**验收**：收盘后访问持仓页，Network 面板可见刷新间隔变化。

### E 组 · 资金面增强（P2）

> 本组依赖东财接口可用性，每项先做接口验证（curl 实测），不可用则降级或跳过并在代码注释中记录。

- **E1 连续性资金流**：个股/板块「连续 N 日主力净流入」排行。历史逐日资金流可尝试东财 `push2his` 的资金流历史接口；若不可得，改为「客户端逐日累积」（同 A2 的落盘思路：每日 15:30 抓取当日板块/个股资金流存 `data/flows/`，累积出连续性）
- **E2 北向资金**：注意 2024 年 8 月后北向实时净买入已停止逐日披露，先验证接口，不可得则展示「陆股通成交总额」或移除
- **E3 融资融券**：东财 datacenter 有融资融券余额接口，展示两融余额 30 日曲线 + 净买入增减
- **E4 游资席位胜率**：龙虎榜历史（东财接口支持按日期翻页），统计知名席位（复用 `/api/youzi` 的 `HOT_SEAT` 正则）近 30 日上榜个股的 T+5 表现

**验收**（E1/E3/E4）：与东财 App 对应页数值抽样一致。

### F 组 · 合规提示（P3）

**F1**：预测PP / 高抛低吸信号 / 模拟持仓三页固定展示：「数据仅供研究，不构成投资建议」；预测PP 页同时展示 A1 回测与 A2 追踪的真实胜率数据（有数据比空口免责更可信）。

---

## 4. 统一口径定义（所有计算必须遵循）

| 指标 | 定义 |
|---|---|
| 日收益 | `r_t = c_t / c_{t-1} − 1`，c 为**后复权**收盘价 |
| 区间收益 | `c_{t+n} / c_t − 1` |
| 年化波动率 | `std(r) × √252` |
| Beta | `cov(r_i, r_m) / var(r_m)`，m = 沪深300（1.000300），窗口 250 日 |
| 夏普比率 | `(年化收益 − 0) / 年化波动率`（无风险利率取 0，简化口径，前端注明） |
| 最大回撤 | `max(1 − nav_t / max_{s≤t} nav_s)`，nav 为净值序列 |
| 胜率 | 收益 > 0 的样本占比 |
| T+N 收益 | T 日收盘买入 → T+N 日收盘卖出的区间收益（N 为交易日） |
| 相关性 | Pearson 相关系数，近 120 日日收益 |
| 佣金 | 万 2.5（0.025%），单笔最低 5 元，买卖双向 |
| 印花税 | 卖出 0.05% |
| 过户费 | 0.001% 双向 |
| 双边成本 | 佣金×2 + 印花税 + 过户费×2（按万元级金额估算约 0.11%~0.15%，代码中按实际公式算） |

> 手续费常量统一定义在一处（`server.js` 顶部 `FEE` 对象 + 前端 `api.js` 镜像），禁止各处散落魔法数字。

---

## 5. 技术约束（硬性）

1. **零 npm 依赖**：不新增任何 dependencies（`crypto`/`zlib` 为 Node 内置可用；nodemailer 保持现状的可选依赖）
2. **上游口径不动**：`server.js` 中所有上游 URL、字段解析、域名降级、注释里的实测记录一律保留，只允许在函数外围新增调用
3. **前端 ES5 风格**：与现有代码一致（`var`/`function`），新增路由走 `ROUTES` 表注册
4. **Node 18+** 语法；服务重启不丢 `data/` 下数据
5. **不破坏现有 11 个页面**功能与前后端数据结构；`scrypt` 同步键算法不变
6. 大计算量接口（回测/相关性）必须走 `mapLimit` 限并发 + 内存缓存，**不得绕过 `gateAcquire` 闸门直连上游**
7. K 线单次请求上限 500 根；回测窗口超出时用「分批拉取 + 拼接」且拼接处去重（按日期）

---

## 6. 执行顺序与交付

```
A1 回测系统 → A4 可买性过滤 → A3 择时过滤（需 A1 对比数据）→ A2 追踪落盘
→ D1 复权（A 组依赖，可与 A1 同批实现）→ B1 风险指标 → C 持仓升级
→ B2/B3 → E 组 → A5 → F1
```

> 注：D1 的 hfq 支持实际是 A1 的前置，实现 A1 时一并完成。

每完成一项：
1. 对照该项「验收标准」逐条自检
2. 输出：变更文件清单 × 对应需求编号 × 自检结果（附证据：接口返回片段 / 计算样例 / 截图说明）
3. 全部完成后跑一次 11 页面手工回归 + `node server.js` 零报错启动
